{
	"scenario": {
		"name": "python-discount-bug",
		"language": "python",
		"description": "Gold tier discount multiplier is 1.0 (100%) instead of 0.1 (10%), causing gold customers to receive a 100% discount",
		"timeout_seconds": 120,
		"max_budget_usd": 0.5
	},
	"setup": {
		"commands": []
	},
	"visible_test": {
		"command": "python3 -m pytest test_discount.py -x -q 2>&1"
	},
	"validation": {
		"command": "python3 -m pytest test_validation.py -x -q 2>&1"
	}
}
