{
	"scenario": {
		"name": "node-event-loop-order",
		"language": "node",
		"description": "Config is loaded via a .then() microtask but read synchronously before the microtask executes, so config is always null",
		"timeout_seconds": 240,
		"max_budget_usd": 1.00
	},
	"setup": {
		"commands": []
	},
	"visible_test": {
		"command": "node --test test-config.js 2>&1"
	},
	"validation": {
		"command": "node --test test_validation.js 2>&1"
	}
}
