{
	"scenario": {
		"name": "node-expense-tracker",
		"language": "node",
		"description": "Expense tracker reports wrong monthly totals due to three bugs: date filter ignores year, refunds counted as positive, category rollup fails on whitespace",
		"timeout_seconds": 420,
		"level": 3
	},
	"setup": {
		"commands": []
	},
	"visible_test": {
		"command": "node --test test-reports.js 2>&1"
	},
	"validation": {
		"command": "node --test test_validation.js 2>&1"
	}
}
