{
	"scenario": {
		"name": "node-float-accumulation",
		"language": "node",
		"description": "Exact float equality check triggers a correction that adds a tiny residual to the last share, causing totalShares to differ from totalWithTip after rounding",
		"timeout_seconds": 300,
		"level": 1
	},
	"setup": {
		"commands": []
	},
	"visible_test": {
		"command": "node --test test-bill.js 2>&1"
	},
	"validation": {
		"command": "node --test test_validation.js 2>&1"
	}
}
