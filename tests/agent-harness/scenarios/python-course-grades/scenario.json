{
	"scenario": {
		"name": "python-course-grades",
		"language": "python",
		"description": "Course grading system gives wrong final grades due to three bugs: drop-lowest drops highest scores, early submissions get late penalties, and grade boundaries exclude exact matches",
		"timeout_seconds": 420,
		"level": 3
	},
	"setup": {
		"commands": []
	},
	"visible_test": {
		"command": "python3 -m pytest test_grades.py -x -q 2>&1"
	},
	"validation": {
		"command": "python3 -m pytest test_validation.py -x -q 2>&1"
	}
}
