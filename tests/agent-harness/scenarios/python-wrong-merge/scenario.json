{
	"scenario": {
		"name": "python-wrong-merge",
		"language": "python",
		"description": "A stable merge uses >= instead of > in the comparison, so equal-key elements from the second list appear before those from the first",
		"timeout_seconds": 120,
		"max_budget_usd": 0.5
	},
	"setup": {
		"commands": []
	},
	"visible_test": {
		"command": "python3 -m pytest test_merge.py -x -q 2>&1"
	},
	"validation": {
		"command": "python3 -m pytest test_validation.py -x -q 2>&1"
	}
}
