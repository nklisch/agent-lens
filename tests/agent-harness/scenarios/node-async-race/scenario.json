{
	"scenario": {
		"name": "node-async-race",
		"language": "node",
		"description": "An async function calls fs.writeFile without await, causing a race: the subsequent read sees stale data",
		"timeout_seconds": 180,
		"max_budget_usd": 0.75
	},
	"setup": {
		"commands": []
	},
	"visible_test": {
		"command": "node --test test-cache.js 2>&1"
	},
	"validation": {
		"command": "node --test test_validation.js 2>&1"
	}
}
