{
	"scenario": {
		"name": "node-mutation-before-read",
		"language": "node",
		"description": "Promotional prices overwrite catalog prices before the average original price is calculated, so the average includes mutated values",
		"timeout_seconds": 240,
		"max_budget_usd": 1.00
	},
	"setup": {
		"commands": []
	},
	"visible_test": {
		"command": "node --test test-promotions.js 2>&1"
	},
	"validation": {
		"command": "node --test test_validation.js 2>&1"
	}
}
