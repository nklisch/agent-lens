The `applyPromotions` function in `promotions.js` is returning the wrong `avgOriginalPrice`. It should report the average of the original catalog prices before promotions were applied, but the value is lower than expected.

The test in `test-promotions.js` demonstrates the failure. Debug this issue and fix the bug so that `test-promotions.js` passes.
