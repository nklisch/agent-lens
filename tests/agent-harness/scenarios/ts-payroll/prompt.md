Sarah Parker's pay stub shows net pay of $1,862.00 but it should be $1,814.00. The payroll department says overtime, tax, and deduction calculations all seem slightly off but they can't pinpoint the issues.

The source files are `types.ts` (data structures), `data.ts` (employee and config data), `tax.ts` (tax brackets), `overtime.ts` (overtime calculation), `deductions.ts` (deduction processing), `payroll.ts` (pay stub generation), and `reports.ts` (payroll summaries). Run `npx tsx --test test-payroll.ts` to see the failing test.
