{
	"scenario": {
		"name": "python-billing-calc",
		"language": "python",
		"description": "SaaS billing produces wrong invoice totals due to two interacting bugs: a tier boundary off-by-one and missing sub-feature usage aggregation",
		"timeout_seconds": 360,
		"level": 2
	},
	"setup": {
		"commands": []
	},
	"visible_test": {
		"command": "python3 -m pytest test_billing.py -x -q 2>&1"
	},
	"validation": {
		"command": "python3 -m pytest test_validation.py -x -q 2>&1"
	}
}
