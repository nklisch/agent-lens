{
	"scenario": {
		"name": "ts-access-control",
		"language": "typescript",
		"description": "Access control system grants wrong permissions due to role inheritance traversal stopping early and permission merge using spread instead of deep merge",
		"timeout_seconds": 360,
		"level": 2
	},
	"setup": {
		"commands": []
	},
	"visible_test": {
		"command": "npx tsx --test test-access.ts 2>&1"
	},
	"validation": {
		"command": "npx tsx --test test_validation.ts 2>&1"
	}
}
