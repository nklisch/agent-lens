{
	"scenario": {
		"name": "ts-payroll",
		"language": "typescript",
		"description": "Payroll system produces wrong net pay due to three bugs: flat-rate tax instead of progressive brackets, PTO hours counted as overtime, and pre-tax deductions subtracted twice",
		"timeout_seconds": 420,
		"level": 3
	},
	"setup": {
		"commands": []
	},
	"visible_test": {
		"command": "npx tsx --test test-payroll.ts 2>&1"
	},
	"validation": {
		"command": "npx tsx --test test_validation.ts 2>&1"
	}
}
