{
	"scenario": {
		"name": "node-data-pipeline",
		"language": "node",
		"description": "Transaction processing pipeline produces wrong monthly summaries due to date format confusion and amount parsing truncation",
		"timeout_seconds": 360,
		"level": 2
	},
	"setup": {
		"commands": []
	},
	"visible_test": {
		"command": "node --test test-pipeline.js 2>&1"
	},
	"validation": {
		"command": "node --test test_validation.js 2>&1"
	}
}
