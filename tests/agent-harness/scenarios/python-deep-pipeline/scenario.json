{
	"scenario": {
		"name": "python-deep-pipeline",
		"language": "python",
		"description": "A bundle discount is never applied because the discount stage checks raw lowercase SKUs but bundle definitions use normalized uppercase SKUs from the catalog",
		"timeout_seconds": 360,
		"max_budget_usd": 2.0
	},
	"setup": {
		"commands": []
	},
	"visible_test": {
		"command": "python3 -m pytest test_pipeline.py -x -q 2>&1"
	},
	"validation": {
		"command": "python3 -m pytest test_validation.py -x -q 2>&1"
	}
}
