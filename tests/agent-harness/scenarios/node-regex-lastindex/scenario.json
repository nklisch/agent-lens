{
	"scenario": {
		"name": "node-regex-lastindex",
		"language": "node",
		"description": "A RegExp with the g flag has stateful .lastIndex, causing .test() to alternate between true and false for the same valid input",
		"timeout_seconds": 300,
		"level": 1
	},
	"setup": {
		"commands": []
	},
	"visible_test": {
		"command": "node --test test-parser.js 2>&1"
	},
	"validation": {
		"command": "node --test test_validation.js 2>&1"
	}
}
