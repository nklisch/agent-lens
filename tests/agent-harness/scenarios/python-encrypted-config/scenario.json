{
	"scenario": {
		"name": "python-encrypted-config",
		"language": "python",
		"description": "Config merge applies env-var overrides after file overrides instead of before, so env-var values silently win over file config values",
		"timeout_seconds": 360,
		"max_budget_usd": 2.0
	},
	"setup": {
		"commands": []
	},
	"visible_test": {
		"command": "python3 -m pytest test_config.py -x -q 2>&1"
	},
	"validation": {
		"command": "python3 -m pytest test_validation.py -x -q 2>&1"
	}
}
